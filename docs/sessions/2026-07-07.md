# Session: 2026-07-07

## Goal

Full code review of the Menu Items editor (Waves 1–4 on `feature/menu-editor`): find bugs, fix
what's worth fixing, audit for missing features, and document — the review-feedback input Wave 5
(T11) was waiting for.

## Progress

- **Reviewed the whole branch diff** (`main...HEAD`, ~1,845 lines / 12 files) two ways: a full
  manual read of every changed file + its integration context, and an independent deep-review
  agent pass over the same diff. Also validated all 89 curated SF Symbol names at runtime
  (zero invalid) and ran clean signed builds before/after.
- **Found 7 bugs (2 critical, 4 important, 1 minor) — all fixed and build-verified.**
  Full detail with file refs in `docs/MENU_EDITOR_REVIEW.md`; headlines:
  1. *Critical:* preview's "+ add" ghost clamped onto the last real wedge and stole its clicks
     on the default 4/4 config (`RingPreviewSelector`) — ghost now only renders on a genuinely
     empty spoke.
  2. *Critical:* a present-but-undecodable `config.json` was silently replaced with sample
     defaults, which autosave then persisted over the real file; closing before first load
     could flush an empty model (wiping rings + trigger). New `LoadState` gate: saves only
     after a successful load; decode failure → alert, saving locked.
  3. *Important:* spoke cap was global (`max(inner,middle) ≥ 8`) instead of per-band —
     middle at 8 wrongly blocked adding inner items. New `atCap(for:)`.
  4. *Important:* editor autosave merged into the window-open `baseConfig` snapshot,
     reverting Settings changes (appearance/triggers) made while the editor was open —
     now re-reads disk as merge base at save time (same pattern as `RingAppearanceSettingsView`).
  5. *Important:* close→reopen race could resurrect stale rings over a just-flushed edit —
     flush now lives in `saveTask`; `refreshFromDisk()` awaits it.
  6. *Important:* switching action type kept the old type's `actionData` (UI showed
     "Snap to: Left", runtime silently no-oped) — model invariant 4 resets carried-over
     payloads in the binding setters (not `.onChange`, which would wipe data on selection switch).
  7. *Minor:* every window reopen armed another self-re-arming observation chain — single
     chain enforced.
- **Enhancement:** `AppPickerSheet` icons now load lazily per row (was: hundreds of
  synchronous `NSWorkspace.icon` calls blocking sheet presentation).
- **Feature-gap audit** written to `MENU_EDITOR_REVIEW.md` §5 (undo/redo, duplicate item,
  symbol search, test-command button, action-failure feedback, orphaned `keyboardShortcut`
  field, config import/export, per-app profiles…).
- **Environment fix:** signed Debug builds were broken on the M4 Pro — `Debug.local.xcconfig`
  contained the **M1 Max's** cert SHA (carried over despite the sync-ignore intent). Rewrote
  with this Mac's SHA per the `Debug.xcconfig` bootstrap; signed build verified. Memory
  (`apple-developer.md`) updated with current per-Mac SHAs.

## Decisions

- **Editor persistence safety model** (logged in `decisions.md`): (a) all config saves are
  read-modify-write against a *fresh* disk read, never a cached snapshot; (b) saves are gated
  on a successful load (`LoadState`), so a corrupt or not-yet-loaded config can never be
  overwritten with defaults.

## Next

- **Wave 5 remainder:** T11 is now largely closed by this review; the one open T11 item is
  **SF-symbol validation gating persistence** (invalid names are flagged inline but still
  autosaved → blank wedge). Needs a design choice: keep-last-valid vs revert-on-blur vs
  persist-with-placeholder. Then **T12 user-driven live verify** (no synthetic input) per
  `MENU_EDITOR_PLAN.md`, then merge `--no-ff` to `main`.
- Known accepted risks to revisit later: quit-during-debounce (<400ms) edit loss; preview
  ignores custom appearance radii; 448pt preview squeezes the form at min window height
  (`MENU_EDITOR_REVIEW.md` §3).

## Sync

- Branch `feature/menu-editor`, first push to origin this session (branch previously had no
  upstream — was invisible to the M1 Max). Commits: retroactive 2026-06-02 docs, review fixes,
  session docs. Working tree clean after wrap-up.
- **This Mac's clone had NO git remote at all** — added `origin`
  (`https://github.com/Xpycode/MousePlus.git`, HTTPS via gh) during wrap-up.
- **⚠️ OPEN DECISION — remote `main` is a dead snapshot:** GitHub `main` = 4 commits ending
  2026-04-26 (`1feda96`), sharing **zero history** with local `main` (`17317fc`; repo was
  re-initialized fresh 2026-05-30, root `fe9dd23`). Remote `main` can never fast-forward.
  NOT force-pushed (multi-Mac Rule 1). Likely fix, pending user go-ahead:
  `git branch archive/pre-reinit origin/main && git push origin archive/pre-reinit` (optional
  archive), then `git push --force-with-lease origin main`. Until then, only
  `feature/menu-editor` is current on GitHub; the M1 Max should fetch that branch, not main.

---

# Session: 2026-07-07 (part 2 — Wave 5 close-out)

## Goal

Resolve the one open T11 design choice (SF-symbol validation vs autosave) and implement it, then
tighten the editor's ergonomics. Part 1 (the code review) left T11 needing a decision + T12 verify.

## Progress

- **T11 decision made + implemented** (see Decisions): invalid SF Symbol names no longer blank a
  wedge. New `Utilities/SFSymbol.swift` — single source of truth `isValid` / `placeholder`
  (`questionmark.circle`) / `resolved`. Every `Image(systemName:)` drawing a user-supplied icon now
  routes through `SFSymbol.resolved()`: `WedgeView` (both glyph sites), `SlotEditorForm` sub-item
  rows. `SymbolField.isValidSymbol` forwards to `SFSymbol.isValid` so the field's red-flag rule and
  the ring's fallback share one definition. One fix covers the live ring, the editor's WYSIWYG
  preview (embeds the real `RingMenuView`), and any config source. Commit `ea4a2fc`.
- **Editor relaid out side-by-side.** Ring + detail form were stacked (fixed ~448pt ring pushed the
  form below the fold → scroll to reach). Moved into a draggable `HSplitView` (ring left, form
  right, per the project's shell convention); Inner/Middle toggle + Add/Reset/Done bars stay
  full-width. Window 560×620 → initial 960×660 / min 900×600 (`MenuEditorWindowController`;
  `contentMinSize` also clamps an older autosaved narrow frame up). Commit `ca223b5`. User: "better".
- Both changes **build-verified** (clean signed Debug build) and relaunched live.
- **Trigger setup on this Mac (M4 Pro):** this clone had **no `config.json`** (fresh-install
  state) — the app now ships `TriggersConfig` unbound, so nothing opened the ring. Confirmed via
  the Identify Input window that Accessibility is granted and the chord is detected (Key 46 +
  Ctrl/Opt/Cmd), then recorded the keyboard trigger in Settings → Triggers. `config.json` now holds
  `Ctrl+Opt+Cmd+M` tap-toggle (keyCode 46 / mods 1835008); ring opens. App-Support config isn't
  Syncthing-synced, so each Mac binds its own trigger.

## Decisions

- **T11 invalid-symbol handling = persist-with-placeholder + defensive render** (chosen over
  keep-last-valid and revert-on-blur). Full ADR in `decisions.md`. Crux: keystrokes can't be gated
  (a valid name is typed through invalid prefixes) and there's no Save button to gate (autosave), so
  the "no blank wedge" guarantee belongs at the render layer, not on input.

## Next

- **T12 user-driven live verify** still outstanding (no synthetic input): edit both rings, add an
  app, set a `windowSnap` zone, reorder, add/remove sub-items, Reset; confirm the live ring
  (`Ctrl+Opt+Cmd+M`) reflects edits + persists across quit/relaunch; re-test the part-1 fixed flows
  (Settings-while-editor-open, close→quick-reopen) and the new placeholder behavior with a bad icon.
- Then merge `feature/menu-editor` `--no-ff` to `main`.
- Deferred: make the ring preview scale to fill its `HSplitView` pane (currently fixed-size/centered).
- **Still open (unchanged):** the dead-snapshot remote `main` reconciliation (part-1 Sync).

---

# Session: 2026-07-07 (part 3 — Fable adversarial panel + data-loss fixes)

## Goal

Before running T12 / merging, get a **second, independent** review of the `feature/menu-editor`
diff from a different model (user's idea: "should we have Fable a look?"). Then fix whatever's
worth fixing.

## Progress

- **Ran a four-lens Fable-5 adversarial panel** over the same `main...HEAD` diff — four independent
  reviewers, one lens each (*correctness*, *model-invariants*, *persistence-safety*,
  *SwiftUI-identity*), each handed `MENU_EDITOR_REVIEW.md` §1 so it hunted for what the first pass
  **missed** rather than re-flagging the 7 fixed bugs. **15 new findings**, full table in
  `MENU_EDITOR_REVIEW.md` §7.
- **The panel earned its keep on a disagreement:** the *correctness* lens certified the
  `refreshFromDisk`/close-flush path clean ("fix #5 holds"); *persistence* + *SwiftUI-identity*
  refuted it. A direct code read confirmed the refuters — **F1 is real**: `await saveTask.value` is
  treated as "the edit is saved," but a debounce task **cancelled** by the next edit completes that
  await at its `isCancelled` return **without ever writing** → a reopen-during-debounce reloads
  stale disk and persists the reverted rings. A lone same-model reviewer would have greenlit it.
- **Fixed clusters A + B + D (build-verified, `** BUILD SUCCEEDED **`); 6 findings deferred as
  logged risks.**
  - **A — load/save path (`MenuEditorWindowController`):** new `isDirty` flag; `flushPendingSave()`
    persists deterministically instead of `await`-ing a possibly-cancelled task; `refreshFromDisk`
    refuses to overwrite the working model from disk while `isDirty` (local edits win) and skips
    `model.load` when disk == model (no churn; selection/open sheet preserved); `saveNow` now
    **bails + alerts on a decode-throw** instead of clobbering with `baseConfig`, and **surfaces
    write failures** (`presentSaveFailure`) instead of swallowing them; a clean close writes
    nothing. Kills **F1–F4** (all data-loss) **+ F11**.
  - **B — model normalization (`MenuEditorModel`):** `normalizeSnapPayloads()` (run in
    `init`/`load`/`resetToDefaults`/after `removeSubItem`) gives a *direct* `.windowSnap` item a real
    zone so it can't no-op behind a "Left" label (**F7**, invariant 4); parent markers with
    sub-items are skipped to avoid on-disk churn. New `maxSubItems = 12` cap (clears the 10-zone
    Snap default) gates `addSubItem` + disables the "Add Sub-item" button (**F8**, outer band).
  - **D — render site (`RingPreviewSelector:220`):** `SFSymbol.resolved(sub.icon)` — the one T11
    draw site the part-2 fix missed (**F15**).
- **All 15 findings recorded** in `MENU_EDITOR_REVIEW.md` §7 (fixed vs. deferred, with repro).

## Decisions

- **Fix the 5 normal-use data-loss paths (A) + the invariant/normalize gaps (B) + the T11 miss (D)
  now; defer the rest.** Deferred (logged §7, accepted risk): **F5** (both Settings writers —
  `SettingsView`/`TriggersSettingsView` — still wipe a corrupt config to sample-defaults, so fix
  #2's "your file is protected" alert is only true for the editor; **pre-existing, out of this
  branch's diff**), **F6** (editor↔Settings last-writer-wins race), **F9/F10** (load-time cap &
  depth-cap bypass, hand-edit-gated), **F12** (detail-form `.id`), **F13** (per-keystroke
  `RingViewModel` alloc), **F14** (8-spoke hotspot overlap).
- **Process:** a cross-model adversarial panel is worth running as a merge gate — it found 5
  confirmed data-loss bugs a same-model review missed, and cross-lens agreement (2–3 lenses on the
  same finding) is the strongest real-vs-noise signal.

## Next

- **T12 user-driven live verify (no synthetic input)** — the app is built + relaunched; editor at
  Settings → "Menu Items" → "Open Menu Editor…". Run the standard sweep **plus** targeted
  regression checks for the fixes: **F1** (edit → reopen-while-open → edit again → earlier edit must
  not revert; survives quit/relaunch); **F7** (delete all Snap sub-items → wedge snaps Left, not
  dead); **F8** (spam Add Sub-item → disables at 12); **F15** (bad symbol on a sub-item → placeholder
  glyph, not blank); **F3** (reopen a few times without editing → nothing changes).
- **If T12 passes:** commit Wave 5, log `MENU_EDITOR_PLAN.md` §9 decisions in `decisions.md`, merge
  `feature/menu-editor` `--no-ff` to `main`.
- **Follow-up branch worth scheduling:** the deferred F5/F6 Settings-writer data-loss (parity with
  fix #2 + a serialized config writer).
- **Still open (unchanged):** the dead-snapshot remote `main` reconciliation (part-1 Sync).

## Resume  (next session starts cold)

- **Working tree DIRTY, uncommitted** on `feature/menu-editor` (in sync with origin — no unpushed
  commits). 5 modified files, **build-verified but NOT yet runtime-verified (T12 pending)**:
  - `Controllers/MenuEditorWindowController.swift` — fix cluster A
  - `ViewModels/MenuEditorModel.swift` — fix cluster B
  - `Views/MenuEditor/RingPreviewSelector.swift` — fix D (F15)
  - `Views/MenuEditor/SlotEditorForm.swift` — fix B (F8 button gate)
  - `docs/MENU_EDITOR_REVIEW.md` — §7 panel findings
- **Exact pickup point:** run **T12** (checklist above). These are timing/ordering fixes, so verify
  by hand — the failure windows (reopen mid-debounce, delete-then-commit) can't be reproduced
  synthetically. If clean → commit Wave 5 + `decisions.md` §9 + `--no-ff` merge. If a fix
  misbehaves, the panel's full repros are in `MENU_EDITOR_REVIEW.md` §7.
- **Not committed on purpose** — commit is gated on T12 passing, per the Wave 5 plan.
