# Session: 2026-04-29 c

## Goal

Resume W3 end-to-end mouse-button verification after Karabiner-Elements uninstall + reboot.

## Context

- Previous session: 2026-04-29-b (W3 architecture done; verification blocked on Karabiner DriverKit eating extended buttons; user rebooting after uninstall)
- Current phase: implementation
- Build: clean

## Progress

### Post-reboot DriverKit check

```
$ systemextensionsctl list | grep -i karabiner || echo "✓ no Karabiner DriverKit resident"
✓ no Karabiner DriverKit resident
```

System extension is gone. The W3 environmental blocker is cleared on this machine.

### Clean build cycle

Per project Xcode protocol — kill → clean → build → launch:

- `killall MousePlus` — done
- `xcodebuild clean -workspace 01_Project/MousePlus.xcworkspace -scheme MousePlus -configuration Debug` → **CLEAN SUCCEEDED**
- `xcodebuild ... build` → **BUILD SUCCEEDED**, signed with stable Apple Development cert (SHA-1 `2D26CB1211F32FD4E3C6EF413EC1EDD6F30631AA`, Team `H56HM4MMZS`) — TCC grants from prior sessions preserved
- App launched, PID 4157

### Pivot: testing venue change

User is currently remoting into this Mac, so a hands-on mouse-button test isn't practical from the remote session. Decided to verify on a physical second Mac instead — that machine has never had Karabiner-Elements installed, which makes it the cleanest possible W3 baseline (no DriverKit residue, no cached TCC oddities, no vendor-software interaction).

This is actually a stronger validation than testing post-uninstall on this machine.

### Decisions Made

- **Defer W3 verification to a Karabiner-never-installed Mac** rather than retest on the post-uninstall remote machine. Cleaner signal; rules out any lingering DriverKit residue.

### Blockers

Verification deferred until user is at the second Mac. No code blocker.

## Next Session

1. **On the second Mac** (Karabiner-never-installed): clone or pull the repo, run the same clean build cycle, launch
2. **Open Identify Input** from menu bar → press button 4 / 5 / gesture / DPI / side buttons
3. Report which buttons fire — that confirms NSEvent global monitors are sufficient on a clean macOS install (and validates the v1.1 CGEventTap plan is the right call for the Karabiner-installed slice of users, not a paranoid over-engineer)
4. **Lock W3 default** to whichever button the user prefers
5. **Begin W4** — Settings → Triggers tab with embedded `InputRecorderView`-style recorder, per-binding mode toggle, permission banner, interception-detection warning

## Notes

- The build artifact lives at `~/Library/Developer/Xcode/DerivedData/MousePlus-hbrauxrsfhyxtxhlhgdfthyalmmi/Build/Products/Debug/MousePlus.app` on this machine — won't transfer to the second Mac, which needs its own fresh build (different DerivedData hash, different TCC identity unless signed with the same Apple Development cert).
- If the second Mac uses a different Apple ID for development, plan for one-time TCC re-grant (Accessibility + Input Monitoring) — `tcc-adhoc-signing.md` memory covers the stable-signing pattern that prevents *re-grants on every rebuild*, but doesn't span machines.
