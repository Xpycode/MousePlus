# Session: 2026-09-05 — HUD Motion v1 Wave 5

## Goal and status

Execute the final Motion v1 closure gate. Automated verification and independent review passed;
the user accepted the exact signed build with “looks good.” Task 5.1 and all five waves are closed.

## Verification

- Focused motion, HUD, configuration, Settings coordinator, native controls, and accessibility:
  **91/91 passed**, no failures or skips, confirmed through the `.xcresult` summary.
- Complete scheme test plan: **272/272 passed**, no failures or skips, including runtime interaction
  coverage and one UI app-launch smoke test. That UI test does not validate motion or VoiceOver.
- Authoritative Debug build and `git diff --check` pass. No application source changes were needed.
- `codesign --verify --deep --strict` passes. Signing authority:
  `Developer ID Application: GREGOR MÜLLER (FDMSRXXN73)`.
- Sandbox-only Xcode workspace and certificate trust errors cleared when the same operations ran
  with access to Xcode/macOS services; no project/signing configuration was changed.

Logs: `/tmp/mouseplus-wave5-focused.log`, `/tmp/mouseplus-wave5-full-tests.log`, and
`/tmp/mouseplus-wave5-build.log`.

Result bundles under
`~/Library/Developer/Xcode/DerivedData/MousePlus-hbrauxrsfhyxtxhlhgdfthyalmmi/Logs/Test/`:
`Test-MousePlus-2026.09.05_00-06-18-+0200.xcresult` (focused),
`Test-MousePlus-2026.09.05_00-06-45-+0200.xcresult` (full).

## Adversarial and performance inspection

An independent read-only reviewer and the execution agent found no concrete motion defects:

- Hover animates only emphasis, border opacity, dimming, and subtle icon scale. Content angles,
  radii, caption width, and centroids remain final.
- Branch identity excludes hover, so pointer sweeps do not restart replacement transitions.
  Outgoing snapshots freeze icons, labels, geometry, and accessibility metadata; removal hides
  accessibility, and activation checks the current parent and complete item value.
- Summon/reveal use one-shot state changes. Twelve full-circle wedges share one reveal progress
  and keep their final angles; there is no cascade or repeating timer/timeline in the motion paths.
- Collapse discards the transition owner. Native mouse-up, action dispatch, final panel placement,
  and immediate dismissal do not await animation.
- Preview disables runtime motion; Reduce Motion resolves spatial effects to opacity/instant.

These are source-level findings, not a Core Animation profile or a measured smoothness claim.
Actual redraw behavior, rapid twelve-app sweeps, composed SwiftUI transitions, and spoken VoiceOver
order/activation were included in the requested live matrix below. The user supplied overall
acceptance rather than an itemized result report; no instrumented performance or spoken VoiceOver
measurement is claimed.

## Signed live handoff

Gracefully quit the older process (PID 42849), launched the rebuilt bundle, and verified the new
process (PID 65244) uses this exact executable:

`/Users/sim/Library/Developer/Xcode/DerivedData/MousePlus-hbrauxrsfhyxtxhlhgdfthyalmmi/Build/Products/Debug/MousePlus.app/Contents/MacOS/MousePlus`

The requested live matrix covered both trigger modes and fast release; static/twelve-app branches
and Apps→Snap→Apps; Escape/outside/direct-action dismissal; each role/master Off and speed extremes;
Reduce Motion; VoiceOver; smoothness and target/action agreement.

The user responded **“looks good. log to clear”**. Recorded as overall signed-live acceptance and
closure authorization, without inventing separate per-case observations or a profiling result.

## Completion and next

- Closed Task 5.1 and all five Motion v1 waves. Archived Tasks 4.1 and 5.1: 29 archived tasks,
  zero active tasks, one backlog item; overall progress 29/30 (97%).
- Removed the completed root `IMPLEMENTATION_PLAN.md` and temporary `RESUME.md`. The motion design
  rationale remains in `docs/decisions.md`; implementation commits remain in Git history.
- No new architecture decision, reusable pattern, or actionable defect emerged from this closure.
- Next: use `status`, then scope the existing App Switcher backlog item (windows/recent apps,
  search, grouping/paging, app-specific actions, and keyboard/pointer interaction). No new feature
  implementation is authorized by this handoff.

## Resume (pre-clear — HUD Motion v1 closure)

- **Completed:** HUD Motion v1 accepted and closed. Evidence above remains authoritative: 91 focused
  and 272 full-plan tests, independent review, diff check, signed build and signature validation.
- **Uncommitted work:** completed-plan deletion; current project state, task tracker/archive,
  this session log and both session indexes; preserved pre-existing edits in the September 4 log.
  No application source edits or half-done code changes remain. This pre-clear log did not commit,
  merge, or push; preserve the combined documentation changes when resuming.
- **Exact pickup:** `status`, then scope the tracked next App Switcher increment. There is no active
  root implementation plan and no remaining motion task.
- **Deferred counts:** 0 active tasks, 1 confirmed backlog item, 0 new unconfirmed observations;
  2 external blockers remain (Logitech captures/strategy and the tip-jar URL). Neither blocks scoping.
- **Runtime cleanup:** gracefully quit this session's signed MousePlus test build. No installed
  MousePlus bundle exists in `/Applications` or `~/Applications`, so no replacement was launched.
  The unrelated VideoCorruptor test process was left untouched.
- **Git:** `fix/hud-live-remediation`; no remote fetch/reconciliation/push performed, and push status
  is unverified. This is a context-clear handoff, not a cross-Mac synchronization claim.
- **Documentation validation:** `git diff --check` passes. The master index checker reports 16
  older entries missing because it ignores the archive; the combined live/archive check confirms
  all 26 logs are indexed, with no orphan links and 20 live rows. Archive count verified at 29.
- **Model fit:** deep capability + high reasoning for choosing the next feature scope; the host's
  current setting is not reliably visible.

## Continuation — HUD Opening Styles Wave 5

User review found that all Opening animations looked the same. Code review identified two causes in
the shared lifecycle: reset and playback were written in one SwiftUI update, and the parent resolved
only endpoint frames instead of exposing normalized progress as `Animatable`. As a result, spatial
masks could disappear at the endpoint and staggered segment opacities could interpolate together.

Commit `e96ba44` adds a cancellable, view-scoped reset-frame boundary and an animatable shared-clock
content view. A regression test verifies that 50% clock progress resolves a genuine intermediate
Circular Sweep frame.

Post-fix evidence:

- Focused plan suite: **112/112 passed**, zero failures or skips; result bundle
  `Test-MousePlus-2026.09.05_09-10-43-+0200.xcresult`.
- Complete scheme: **290/290 passed**, zero failures or skips; result bundle
  `Test-MousePlus-2026.09.05_09-11-08-+0200.xcresult`.
- Authoritative Debug build and `git diff --check` passed. The initially recorded DerivedData
  artifact proved stale when Finder showed that its bundle predated the correction, so it must not
  be used for corrected-build acceptance.
- A subsequent clean rebuild from the current correction commit succeeded at
  `/tmp/MousePlus-OpeningStyles-Corrected/Build/Products/Debug/MousePlus.app`. Both the bundle and
  executable are timestamped 2026-09-05 10:34:25 +0200; strict deep verification passes with the
  configured Developer ID identity.
- The complete scheme's one UI smoke test launched the app through Xcode despite the user's request
  to continue without controlling it. No further UI/app-hosted checks were run.

Task 5.1 remains open. The corrected artifact still needs user-driven visual/accessibility acceptance;
no live observation of the corrected animations, VoiceOver result, or performance measurement is
claimed.

### Live-remediation continuation

The next user check exposed two further lifecycle defects: the Settings preview animated correctly
but a newly inserted runtime HUD did not, and clicks in the active Settings window neither dismissed
the HUD nor let a repeated mouse tap-toggle close it reliably.

- Runtime opening playback is now armed from the request during `HUDOpeningMotion` initialization,
  so the first view-scoped task cannot finish before an `onAppear` mutation requests playback.
- Local mouse-down handling now distinguishes sibling MousePlus windows from transparent HUD
  corners: a Settings click dismisses and remains deliverable, while a HUD-corner click is consumed.
- The configured mouse tap-toggle bypasses generic outside-click dismissal, leaving one trigger path
  responsible for closing and preventing a queued dismiss-then-reopen race.
- The focused `HUDOpeningMotionTests` and `DismissMonitorTests` run passes **17/17**, zero failures or
  skips; result bundle `Test-MousePlus-2026.09.05_10-50-34-+0200.xcresult`.
- A separate clean Debug build succeeds at
  `/tmp/MousePlus-OpeningStyles-Remediation-Live/Build/Products/Debug/MousePlus.app`. Its bundle and
  executable are timestamped 2026-09-05 10:51:26 CEST, and strict deep signature verification passes.

User follow-up confirmed that Settings clicks now dismiss correctly, but the runtime HUD still
appeared not to play the selected Opening style. A read-only process check then found three MousePlus
instances running concurrently: an older Xcode DerivedData build, the 10:34 corrected build, and the
10:51 remediation build. Each can own the same global trigger, so one press can display stacked HUDs
from different binaries and let an older settled HUD cover the current animation. That invalidates
the animation observation.

A speculative post-mount replay change was implemented and passed 31 focused tests, but was removed
once the process evidence exposed the test contamination; no unverified lifecycle workaround remains.
A fresh clean build of the already-tested remediation succeeds at
`/tmp/MousePlus-OpeningStyles-Isolated-Live/Build/Products/Debug/MousePlus.app`. Its bundle and
executable are timestamped 2026-09-05 11:04:50 CEST, and strict deep signature verification passes.

Task 5.1 remains open: Settings-click dismissal is accepted, while a clean single-process runtime
opening check, repeated tap-toggle, and the broader visual/accessibility matrix remain pending.

The user then repeated the check with exactly one process from the isolated artifact and still saw
only a fast fade in the actual HUD. macOS Reduce Motion was read through AppKit and is `false`, so
accessibility fallback does not explain it. This confirms the live panel's initial SwiftUI insertion
can settle before the selected opening renderer becomes visible. Runtime now changes a guarded
invocation identity one frame after the panel mounts, driving the same explicit replay transition
that works in Settings. Stale callbacks cannot replay a closed or superseded HUD.

- The expanded `HUDOpeningMotionTests`, `RingRuntimeInteractionTests`, and `DismissMonitorTests` pass
  **31/31**, zero failures; result bundle
  `Test-MousePlus-2026.09.05_11-10-18-+0200.xcresult`.
- The clean Debug artifact at
  `/tmp/MousePlus-OpeningStyles-PostMount-Live/Build/Products/Debug/MousePlus.app` is timestamped
  2026-09-05 11:10:58 CEST and passes strict deep signature verification.

The user then confirmed that the selected animation now works in the actual HUD. That validates the
guarded post-mount replay, but exposes the next concrete lifecycle defect: at the beginning of the
animation, the entire settled HUD is visible for a very brief flash. This is now Task 5.1's active
acceptance blocker. No speculative flash fix is included in this log. The next implementation step is
to mount animated runtime content concealed and non-playing before arming exactly one guarded
post-mount replay, while keeping Off and accessibility-resolved instant presentation immediately
visible. The existing 31-test result and signed artifact predate that future fix.

## Concealed initial mount remediation

The user requested concealed initial mounting and regression coverage. Runtime now supplies an
explicit awaiting-mount request: the renderer starts at progress zero, conceals artwork and center
presentation, and does not arm its clock. The existing invocation-guarded callback resets and reveals
the opening's initial frame before starting one animation. Off/disabled motion stay immediately
visible, while Reduce Motion retains the existing Fade fallback. Native input and accessibility
targets retain their geometry. Interaction or preference changes during mounting settle immediately
and prevent the delayed callback from hiding the HUD again.

Validation for this change:

- The runtime regression first failed on the assertion that mounting must not arm playback;
  the result bundle confirms that exact failure. It passes after the playback fix.
- **51/51 focused tests pass**, zero failures/skips, in
  `/tmp/MousePlus-OpeningStyles-PostMount-Live/Logs/Test/Test-MousePlus-2026.09.05_12-35-05-+0200.xcresult`.
  Includes real-panel/SwiftUI frame checks for all five animated styles, early interaction and
  preference interruption, plus stale/duplicate mount callbacks and Off/Reduce Motion policies.
  Hosted fixtures explicitly wait for SwiftUI mounting before sending interaction changes.
- A fresh authoritative Debug build succeeds at
  `/tmp/MousePlus-ConcealedMount-Live.mCxNaw/Build/Products/Debug/MousePlus.app`, executable timestamp
  **2026-09-05 12:35:47 CEST**. Strict deep signature verification passes with the configured
  Developer ID identity. `git diff --check` passes. Existing WindowService pointer and UI-test
  sandbox-setting warnings remain.
- The old PostMount-Live directory was reused for tests and no longer contains the untouched
  11:10 handoff artifact; use the new ConcealedMount-Live artifact above for visual review.
- No full-suite rerun or user-app control was performed for this change. Hosted tests check frames
  supplied to artwork, not actual material pixels or spoken VoiceOver. User confirmation of
  flash-free runtime opening, tap-toggle, and the remaining live matrix is still pending.

## Latest user review — fade-only regression / pre-clear

After testing the concealed-mount artifact, the user reported: "back to fade in without flash"
and asked whether the change stopped the other animation. Record the observed result as an open
regression: the initial flash is gone, but the selected runtime opening effect is no longer visible.
The mechanism is unverified; do not present playback suppression, accessibility fallback, or
material compositing as an established cause. The previous selected-animation acceptance applies
to the earlier post-mount replay build, not this concealed-mount build.

The 51 passing tests did not establish the complete visible runtime behavior. The hosted checks
mount the opening owner with clear placeholder content and collect frame values; they do not render
the real ring's materials or exercise all of RingMenuView's settle callbacks. Next-session diagnosis
must cover that missing path and verify the selected effect itself, not merely progress endpoints
or the absence of a flash. No additional implementation, rollback, build, or test run was performed
in this logging turn.

The read-only process check found one MousePlus process, PID 73476, executing
`/private/tmp/MousePlus-ConcealedMount-Live.mCxNaw/Build/Products/Debug/MousePlus.app/Contents/MacOS/MousePlus`.
It was left running for the user; no app control was performed. The live-index checker still flags
16 deliberately archived entries; the combined live/archive check covers all 26 session files with
zero missing or orphan references. Project state, task, plan, and session index now reflect the
fade-only regression. No task was completed or archived; no commit/push was requested or performed.

## Resume (pre-clear — current)

- **Uncommitted work:** the active opening-styles plan/spec plus combined project-state, task/archive,
  session-log, and session-index updates remain uncommitted. These documentation changes include
  preserved work from before the Opening Styles implementation; do not discard or overwrite them.
  Earlier application source and tests are committed through `e96ba44` on
  `fix/hud-live-remediation`; the latest runtime/dismissal remediation source and tests are
  intentionally uncommitted, including the concealed-mount implementation and its regressions.
  No half-applied edit remains; the implementation builds but fails user visual acceptance.
- **Exact pickup:** diagnose why concealed mounting removed the flash but regressed selected runtime
  playback to a fade. Trace `RingMenuView.openingMotionRequest`, `openingIsAwaitingMount`,
  `replayOpening(afterMounting:)`, `HUDOpeningMotion` task/transition handling, `mountWasInterrupted`,
  and actual settle callbacks/material rendering. These are investigation points, not proven causes.
  Improve coverage through the full runtime renderer and restore selected playback without a flash.
  Leave app control to the user. Then verify tap-toggle, Off, Replay, Reduce Motion, keyboard/VoiceOver,
  and normal invocation. Settings-click dismissal remains accepted; selected playback is regressed.
- **Artifact:** `/tmp/MousePlus-ConcealedMount-Live.mCxNaw/Build/Products/Debug/MousePlus.app`
  (fresh build; executable timestamp 2026-09-05 12:35:47 CEST).
- **Evidence:** corrected code passes the earlier 112/112 focused and 290/290 full tests plus the
  latest concealed-mount remediation's 51/51 focused tests, authoritative build, `git diff --check`,
  and strict signature validation. User confirms no flash in this artifact but reports fade-only
  runtime opening; do not treat automated passes as visual acceptance. VoiceOver/performance and
  the remaining live matrix are still unverified.
- **Deferred counts:** 1 active task, 1 confirmed backlog item, and 0 new Inbox observations. Two
  unrelated external blockers remain: Logitech HID++ capture/strategy and the tip-jar URL.
- **Runtime caveat:** one user-operated concealed-mount build was running at the latest read-only
  check (PID 73476, exact path above). It was left untouched. Do not assume a future process count;
  inspect again if needed. Leave app control to the user unless explicitly authorized. Earlier full
  scheme UI smoke testing launched the app despite that constraint; avoid repeating it inadvertently.
- **Session index:** the master live-index checker reports the 16 intentionally archived rows as
  missing; a combined live/archive comparison covers all 26 session logs with no drift. The live
  index remains capped at 20 rows.
- **Git/sync:** no fetch, merge, remediation commit, or push was performed. Commit `e96ba44` contains
  the earlier interpolation fix; the runtime/dismissal remediation is uncommitted. Earlier feature
  commits are `ead81f0`, `f93f4f3`, `5cdebc7`, and `9cf122c`.
- **Model fit:** deep/frontier capability with high or extra-high reasoning — next work is repeated
  SwiftUI/AppKit lifecycle and visible-rendering failure despite green tests. The current reasoning
  setting is not reliably visible. Start with concrete runtime evidence before another workaround.

## Runtime opening diagnosis — full renderer and diagnostic build

Continued Task 5.1 at the user's request. No runtime fix is claimed in this session.

- Added a frame observer to the actual RingMenuView and traced request transitions plus settle
  sources. The first fixed-position test failed because the physical pointer was on a wedge;
  its transient selection later cleared. This was legitimate hover settlement, not proof of an
  initial-layout or panel-placement defect. The earlier placement hypothesis was rejected.
- Repeated the full-renderer check centered on the pointer, with live interactions enabled and
  the production 16 ms replay delay: all five animated styles produced intermediate frames.
- An isolated exploratory test captured the actual ring's material artwork at concealed,
  early, middle, and final stages with independent six/eight-slot rotations. Inspection showed
  a concealed initial bitmap and progressive Staggered Segments, rather than a uniform fade.
  Captures: `/tmp/MousePlus-Opening-{concealed,early,middle,final}.png`. These are AppKit view
  captures, not a screen recording or proof of the user's live behavior. The temporary capture
  test and print instrumentation were removed after inspection.
- Retained two full-renderer regressions: each selected effect reaches intermediate frames with
  concealed artwork/center at mount; a selection through RingMenuView's real callback settles
  immediately and suppresses late replay. Retained tests disable physical hover for repeatability
  and inject selection separately. They do not replace the user-controlled trigger/visual matrix.
- Added bounded Debug-only per-panel diagnostics in `HUDOpeningDiagnostics`: saved style,
  motion enabled/duration, actual effective frame effect, concealed/start/intermediate/endpoint
  evaluations, post-mount replay, and first settle reason. No item/action data, repeating timers,
  or observable-state writes are introduced. Endpoint evaluation can precede interpolation and
  must not be interpreted as completion. Release has no diagnostic owner.

Validation:

- 53 focused tests passed, 0 failed/skipped: opening, preview, runtime interaction, motion policy,
  outer band, and outer branch. Result: `/tmp/MousePlus-OpeningTrace-Focused.xcresult`.
- Strengthened effect/mask/scale and center-concealment assertions then passed in a targeted
  rerun: `/tmp/MousePlus-OpeningTrace-RendererFinal.xcresult`.
- Fresh authoritative Debug build passed. Exact artifact:
  `/tmp/MousePlus-OpeningDiagnostic-Live/Build/Products/Debug/MousePlus.app`.
  Executable/signing timestamp: **2026-09-05 13:17:04 CEST**.
- `codesign --verify --deep --strict` passed with certificate-service access; configured
  Developer ID Application identity and team FDMSRXXN73 were confirmed. The sandbox-only trust
  check failed before the successful unrestricted verification; no signing settings changed.
- `git diff --check` passed. Independent review found no blocking diagnostic-code issue and
  prompted the stronger effect/center assertions. No full-suite rerun, commit, or push.
- No user-app launch, quit, or trigger was performed. A sandbox process query was denied, so
  no fresh claim about the user's running binary/process count is made.

Next: the user quits the prior app and launches the exact diagnostic artifact, selects the
reported style, and reproduces the opening while keeping the pointer still away from screen edges.
Also record whether their original observation involved pointer movement or edge clamping. Then
read the bounded trace without controlling the app:

```bash
/usr/bin/log show --last 10m --style compact --predicate 'subsystem == "com.xpycode.MousePlus" AND category == "HUDOpening"'
```

Inspect selected versus effective effect, presence of an intermediate frame, and the first settle
reason. Restore the selected effect only after the actual runtime difference is identified; retain
concealed mounting and immediate input. Task 5.1 and remaining visual/accessibility acceptance stay
open. Current build is diagnostic, not a claimed fix for the user-reported fade-only behavior.

## Global launch preference and corrected diagnostic compilation

The user explicitly requested a persistent, cross-project default: when a new runnable desktop-app
build is ready, quit every running copy of that app and open the fresh artifact. Saved outside the
managed block in `/Users/sim/.codex/AGENTS.md`; Directions deployment preserves that personal section.
This supersedes the earlier MousePlus-specific instruction to leave app control to the user.
Graceful termination, pending-save protection, and verifying the new executable are part of the
preference. No repeated confirmation is needed.

The user then confirmed the real HUD still fades in the diagnostic artifact. Read-only inspection
verified PID 97681 was running the intended artifact, but its log contained no HUDOpening events.
Found and corrected a diagnostic mistake: the custom Debug.xcconfig did not define Swift's DEBUG
condition, so the supposedly diagnostic owner and callbacks were compiled out. The prior build and
53 test results were valid, but the claim that its executable contained runtime tracing was not.
Added `SWIFT_ACTIVE_COMPILATION_CONDITIONS = $(inherited) DEBUG` to Debug.xcconfig. The only other
existing DEBUG block is an unused RadialGeometry self-check helper; Release remains unchanged.

A fresh build passed and `codesign --verify --deep --strict` passed. Verified HUDOpeningDiagnostics
and its mount/replay format strings in the executable itself. New exact artifact:
`/tmp/MousePlus-OpeningDiagnostic-Enabled/Build/Products/Debug/MousePlus.app`, timestamp
**2026-09-05 13:23:53 CEST**. Gracefully terminated PID 97681, waited for all old bundle-ID instances
to exit, launched the new app, and verified PID 1506's executable matches the new artifact.
The bounded launch helper is `/private/tmp/mouseplus-launch-fresh.swift`.

Requested one further user opening to collect the now-enabled trace. No animation root cause or
fix is established yet. Do not mistake endpoint evaluation for completed visible animation.

## Live trigger distinction and user-verified BetterMouse setup

With tracing genuinely enabled, controlled keyboard shortcut invocation produced full 0.5-second
Staggered Segments playback. Subsequent live user attempts included failing invocations whose first
pointer-hover callback settled progress zero before post-mount replay. The user identified the
important distinction: keyboard shortcut works; their direct mouse trigger appeared as Fade.

Added bounded trigger-position, mounted local pointer/center, and first settle-pointer diagnostics.
The incremental build and strict signature check passed. Under the new global preference, quit
PID 1506 gracefully and relaunched the fresh artifact at the same
`/tmp/MousePlus-OpeningDiagnostic-Enabled/Build/Products/Debug/MousePlus.app` path as PID 13567.
Synthetic instantaneous and 0.25-second-held button-4 clicks also produced intermediate frames;
this does not establish the behavior of the user's physical button/remapping stack. A standalone
mouse-event observer recorded zero events; a window-capture attempt failed to locate the HUD and
produced no new live image evidence. Do not treat either as diagnostic proof.

The user proposed mapping their button to the existing **⌃⌥⌘M** shortcut in BetterMouse and removing
MousePlus's native mouse-button binding. Recommended this as a workaround/comparison to avoid
double triggering. The user confirmed: **"works via BetterMouse."** Keyboard and that mapped path
are now user-verified for selected opening playback. Keep their working configuration intact.
Direct native mouse-button triggering remains an open defect; early hover cancellation is observed,
but its coordinate/device cause is not yet established. Full Wave 5 acceptance is not claimed.

No further app rebuild is needed for this documentation update. The standing preference is saved
in the personal section of `/Users/sim/.codex/AGENTS.md`, outside the generated Directions block.

## Session close — commit and push requested

- Synced project state, active task, implementation plan, and session index around the verified
  BetterMouse-to-keyboard setup. Native mouse-trigger opening and remaining Wave 5 acceptance
  stay open; no task was falsely completed or archived.
- Reviewed the pending runtime concealment/replay, same-app dismissal/tap-toggle ownership,
  Debug diagnostics, regression coverage, and preserved Motion v1/opening-styles documentation.
  All pending repository changes belong to this ongoing work and are included in the closeout.
- Validation remains the recorded 53 focused tests plus the strengthened renderer test rerun,
  fresh Debug builds, strict signature verification, and live trigger observations. No additional
  application edits or test reruns were needed for this logging-only closeout. Whitespace checks pass.
- Session-index validation: the master checker sees 16 intentionally archived logs as missing
  because it reads only the live index. Combined live/archive validation covers all 26 log files
  with zero missing/orphan links; the live index contains 20 rows.
- Remote preflight found the working branch ahead by 17 existing commits with no remote-only
  commits; the user requested those commits and this closeout be pushed on
  `fix/hud-live-remediation`. Main is not merged while native-trigger acceptance remains open.
- The user-verified fresh app is intentionally left running under the newly saved global
  quit-old/open-fresh preference. It supersedes the generic log cleanup's installed-app fallback.
- Next pickup: preserve BetterMouse → ⌃⌥⌘M; compare native trigger position and initial hover
  coordinates if resuming that defect. The trace is enabled in Debug.xcconfig and the current
  diagnostic owner. The global Codex preference remains outside this repository.


## Follow-up — stationary-pointer drift and Warp navigation constraint

- User reports that BetterMouse mapping their MX 3S button 5 to the shortcut also stops Warp
  advancing to the next tab. They explicitly request keeping that suppression. Preserve the
  current BetterMouse mapping and MousePlus's disabled direct mouse binding. Do not reproduce
  a native Forward event against Warp. The native NSEvent monitor observes events without
  suppressing foreground-app delivery; this explains the coexistence risk, not an exact physical
  button identity or proof of the animation's full device-specific cause.
- Historical fade-only failures predate the coordinate diagnostics, so those records cannot
  establish a bad native event position. Added bounded Debug source/mode and first-hover samples:
  actual SwiftUI point/center, dead zone, screen point and controller-converted center delta.
  Compare deltas rather than raw local positions because the host includes 12-point padding.
- The first fresh live keyboard check exposed a stationary pointer shifting from centered at
  mount to about 23 points below the center during hover. Added
  `testRuntimePanelKeepsSummonPointCenteredAfterHostedLayoutAndReplay`, using the real renderer,
  controller and fixed screen point, with physical interaction disabled to isolate layout.
  It failed before the fix: hosting bounds changed from a 236-point half-size to 224, and the
  summon point was no longer centered. Disabling sizingOptions alone still failed. Explicitly
  framing the hosted root to the padded square, with sizingOptions disabled, makes it pass.
- Actual change: RingWindowController owns one fixed padded frame for both panel and hosted root.
  No animation playback or mouse-event suppression changes. This fixes demonstrated layout drift;
  it does not constitute physical native-trigger acceptance.
- Validation: initial diagnostic-only focused run 41/41; new regression fails before and passes
  after; complete scheme **303/303**, zero failures or skips. Result bundle:
  `/tmp/MousePlus-OpeningCoordinate-Test/Logs/Test/Test-MousePlus-2026.09.05_14-03-39-+0200.xcresult`.
  Authoritative Debug build and strict signature verification pass. Final app:
  `/tmp/MousePlus-OpeningCoordinate-Live/Build/Products/Debug/MousePlus.app`.
  Gracefully quit the previous diagnostic copy and verified fresh executable PID 59912.
- Final MousePlus-only keyboard checks show SwiftUI and native center deltas agree; actual
  physical pointer movement during one check legitimately interrupted playback. Another final
  invocation records intermediate Staggered Segments frames and its endpoint. Screen-edge
  clamping also legitimately puts the pointer away from center. No physical native button events
  were posted, and persisted triggers still have keyboard enabled / mouseButton none.
  Trace: `/tmp/MousePlus-PanelCenter-LiveTrace.log`.
- Task 5.1 remains open for user visual/accessibility acceptance and isolated physical native-trigger
  verification. Next: use the fresh build through the working BetterMouse shortcut; any future
  native trigger must preserve the user's requirement that Warp does not receive Forward.


## Resume — pre-clear closeout after center-drift fix

- **Scope:** This closeout commits the panel-size fix, bounded trigger/hover diagnostics, failing-then-
  passing regression, and synchronized plan/state/task/session docs on `fix/hud-live-remediation`.
  No unrelated dirty changes or half-finished source edits were found. No merge to main is intended.
- **Validation:** The final application source is unchanged since the 303/303 complete scheme pass,
  successful Debug build, strict signature verification, and recorded live checks. Documentation
  and whitespace checks are the only additional validation needed for this closeout.
- **Next pickup:** Read `docs/PROJECT_STATE.md`, Task 5.1 in `IMPLEMENTATION_PLAN.md`, and this
  follow-up section. Verify the new HUD visually through BetterMouse → ⌃⌥⌘M. Keep MousePlus's
  direct binding disabled and never restore button-5 Forward/next-tab delivery to Warp. If pursuing
  physical native-trigger acceptance, use an isolated setup that preserves this requirement.
- **Evidence boundary:** Stable controller/host geometry is regression-tested; the new layout fix
  is not yet user-accepted on the physical native trigger. Remaining visual, accessibility and
  device/performance acceptance must not be inferred from the test count or keyboard traces.
- **App:** Fresh executable PID 59912 was confirmed as the only running MousePlus copy at closeout:
  `/tmp/MousePlus-OpeningCoordinate-Live/Build/Products/Debug/MousePlus.app`.
  Leave it running under the user's standing fresh-build preference. Local `/tmp` artifacts and
  result bundles do not travel through git; rebuild on another Mac using its configured signing.
- **Deferred:** 1 confirmed Backlog item (next App Switcher scope), 0 Inbox items awaiting reproduction;
  1 open sprint task, with the native-trigger and remaining live checks held in that active task.
  No task is being falsely closed or archived.
- **Index:** Master checker flags 16 intentionally archived log entries; combined live/archive
  validation covers all 26 session files, with zero missing or orphan links and 20 live rows.
- **Sync preflight:** Remote feature branch matches local HEAD before this commit; origin/main has
  no remote-only commits. Commit and push are explicitly requested by the user.
- **Model fit:** Deep capability + high reasoning for remaining hover/lifecycle diagnosis and live
  acceptance; current host setting is not reliably visible.
