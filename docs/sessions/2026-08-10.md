# Session: 2026-08-10

## Goal
Resume toward the menu-editor T12 live-verify, then plan the ③ Send-Keystroke action (user: "plan it using your mcps and agents").

## Progress
- **Git archaeology — the "stale base" warning is a false alarm.** The session-start pre-flight said `origin/main` is 4 ahead of `feature/menu-editor`. Investigated: `origin/main` holds an **orphaned, unrelated April history** (root `5d2a362`: PointerActions→MousePlus rename, GPLv3, xcconfig fix) from before the repo was re-initialized locally on 2026-05-30. The re-founded history (root `fe9dd23`) was never pushed to `origin/main` — only feature branches went up. Local `main` (`17317fc`) is the true base and is **fully contained** in `feature/menu-editor` (merge-base = local main tip). Nothing to merge before T12. ⚠️ Consequence deferred: pushing the merged `main` will be rejected (unrelated histories) — `origin/main` must be **deliberately replaced** at merge time.
- **Clean build + launch for T12** (killall → clean → `BUILD SUCCEEDED` → launched the exact `TARGET_BUILD_DIR` binary). Handed the user the four deferred regression checks (F1 reopen-mid-edit no-revert · F7 de-subitem'd Snap snaps Left · F8 Add-Sub-item disables at 12 · F15 bad symbol → placeholder). **Not yet run/reported** — the session pivoted before verification.
- **Answered "state of shortcuts as trigger":** no first-class keystroke action exists; working stopgap = `.custom` wedge shelling `osascript … System Events keystroke` (e.g. for QuickStatsPanel's ⌃⌥⌘Q); `shortcuts run "Name"` also works via `.custom`; keyboard *ring trigger* (⌃⌥⌘M) + ⌥⌘, Settings hotkey both live.
- **Planned ③ Send-Keystroke** (`docs/SEND_KEYSTROKE_PLAN.md`, 7 tasks / 4 waves, ~1–1.5 d) via a 3-agent research fan-out: (1) codebase map with exact seams (`ActionDataEditor.swift:52` insertion point, `TriggerRecorderService` reusable as-is, zero CGEvent prior art in-repo, §1.5 dismiss seam confirmed unbuilt, dead `RingMenuItem.keyboardShortcut` field left untouched); (2) Apple-docs verification (CGEvent + `.flags` current on 14/15; HID-tap posting makes other apps' `RegisterEventHotKey` fire; Secure Input blocks taps not posting; Mission Control/Spotlight unreachable as plain keycodes); (3) OSS source-reads (Hammerspoon/skhd/espanso/KeyboardShortcuts/Karabiner) — held-modifier contamination defenses, timing floors, `NX_SYSDEFINED` media-key trick, Tahoe ad-hoc-signing event-drop gate.
- **Key plan decisions:** name pinned `ActionType.sendKeystroke`; payload `"keyCode:modifiers"` (NSEvent raw flags) in `actionData`; posting recipe = private CGEventSource + explicit flags on down **and** up + `.cghidEventTap` + 50 ms/10 ms timing; Mission Control = `open -b com.apple.exposelauncher` custom action (not a keystroke); permissions resolved empirically via `CGPreflightPostEventAccess()` (sources conflicted on Input Monitoring vs Accessibility); inner Copy/Paste/Spotlight defaults become real. Formal `decisions.md` entries deferred until built (plan §8).
- Synced `PROJECT_STATE.md` (new track bullet + Next Action 29).

## Decisions
- (Planning-level, logged in `SEND_KEYSTROKE_PLAN.md` §8 — to be written to `decisions.md` when the feature is built, per house convention.)

## Next
1. **T12 live-verify** — the four regression checks above (app is built; relaunch if quit), then the broader editor sweep.
2. `--no-ff` merge `feature/menu-editor` → `main`; log `MENU_EDITOR_PLAN.md` §9 decisions.
3. **Resolve `origin/main`** — deliberately replace the orphaned April history with the real local main (force-with-lease after user confirmation; Rule 1 surfaced, not auto-pushed).
4. Cut `feature/send-keystroke` from merged `main` → `/execute` `SEND_KEYSTROKE_PLAN.md`.
