# Session: 2026-01-12-a

## Goal
Create Xcode project and get basic app running.

## Context
- Previous session: 2026-01-09-a (Discovery interview)
- Current phase: implementation

## Progress

### Completed
- [x] Created Xcode project with scaffold tool
- [x] Configured as menu bar app (LSUIElement = YES)
- [x] Set up folder structure (Models, Views, ViewModels, Controllers, Services)
- [x] Created all base files:
  - Models: ActionType, RingMenuItem, Configuration
  - ViewModels: RingViewModel
  - Views: RingMenuView, SettingsView
  - Controllers: MenuBarController, RingWindowController
  - Services: HotkeyService, ActionService, ConfigurationService
- [x] Fixed build warnings (headermap, Swift 6 actor isolation, unnecessary await)
- [x] Removed unused MousePlusFeature package dependency
- [x] Fixed Settings window to use SwiftUI `openSettings` environment action

### Working
- Menu bar icon visible (hexagon grid)
- About window opens
- Settings/Preferences window opens (3 tabs: General, Hotkey, Menu Items)

### Not Working
- **Ctrl+Space hotkey does not trigger ring menu** - needs investigation

### Discovered
- `nonisolated(unsafe)` needed for PassthroughSubject in actors
- SwiftUI Settings scene requires `@Environment(\.openSettings)` - can't use `sendAction` selectors anymore
- Zombie processes can be held by Xcode's debugserver - kill debugserver to release them

## Blockers
- Hotkey detection not working - likely needs Accessibility permission or event monitor issue

## Next Session
1. Debug why Ctrl+Space hotkey isn't triggering
   - Check if Accessibility permission is granted
   - Verify NSEvent global monitor is receiving events
   - Add logging to HotkeyService
2. Get ring menu to appear at pointer location
3. Test basic ring UI interaction

## Notes
- App runs as menu bar only (no dock icon)
- Build is clean with no warnings
- Project uses PBXFileSystemSynchronizedRootGroup (Xcode 16+) - files auto-sync from disk
