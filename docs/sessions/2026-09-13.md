# Session: 2026-09-13

## Goal

Make unused fixed HUD slots visually transparent and continue app-specific HUD live acceptance.

## Progress

- Confirmed that configured wedges were already omitted from empty slots, but the shared circular
  material backing still painted those slots gray.
- Replaced the full inner/middle backing disk with center material plus per-slot backing for only
  configured inner and middle items. The same occupied-slot policy applies during Staggered
  Segments playback, so empty slots do not flash into view while opening.
- Added a regression proving that backing indices stop at each ring's configured item count even
  when fixed geometry reserves additional slots. The test failed against the old rendering policy
  before the implementation changed.
- Passed 60 focused wedge-presentation, opening-motion, opening-preview, customization-integration,
  and accessibility tests. The authoritative Debug build also succeeded.
- Gracefully quit the prior signed-live build and launched the exact fresh DerivedData artifact as
  PID 18471; the user subsequently confirmed the transparent unused-slot appearance.
- User confirmed the transparent unused-slot appearance, then requested an optional complete-ring
  treatment for people who prefer it. Added a shared, persisted Menu → Empty slots → Fill unused
  slots checkbox, defaulting off; when enabled it completes inner, middle, and visible outer
  material backing without adding actions, hit targets, or accessibility elements.
- Moved the conditional hidden-submenu warning below Outer → Visibility so it no longer shifts the
  Menu/Inner/Middle/Outer selector. All 134 focused customization, rendering, accessibility,
  motion, preview, persistence, and view-model regressions pass, as does the authoritative Debug
  build.
- Gracefully replaced PID 18471 with the exact fresh standalone artifact as PID 30121.
- User live-verified both Fill unused slots states and the relocated warning; the Settings and HUD
  behavior are accepted.
- Session closeout gracefully quit verified DerivedData PID 30121. No installed MousePlus bundle
  exists in `/Applications` or `~/Applications`, so no everyday copy was relaunched; the unrelated
  SoftwareLicensesManager test build was left untouched.

- Resumed E1 with a fresh isolated Debug build at
  `/private/tmp/MousePlus-E1-20260913/Build/Products/Debug/MousePlus.app`; build and strict deep
  Developer ID verification passed. No old MousePlus instance was running; launched PID 50767
  and verified its exact executable path. This build remains available for ongoing live checks.
- User confirmed Finder contextual selection and switching Finder → Global → Finder. Global
  initially disappeared because its saved mode was Hold-release; setting it to Tap-toggle made
  the flow work as expected. No code change was needed. Remaining live checks are still pending.

- User confirmed same-route Tap-toggle dismissal for both Finder and Global, plus opening Settings
  by clicking the Finder HUD center. These checks passed.
- User confirmed stale Hold-release isolation: holding Global, switching to Finder, then releasing
  the old Global chord left Finder open without executing an action.
- User confirmed correct center context indication, dragging in both Finder and Global without
  opening Settings or executing actions, and the Global center Settings click.
- User confirmed Finder/Global switching with Reduce Motion enabled passes without spatial motion
  or delayed response. Their screenshots also confirmed macOS 27 places Reduce motion under
  Accessibility → Motion, in its own panel. Saved that correction in the shared master
  `22_macos-platform.md` for future sessions.

- User confirmed an app without its own HUD stays on Global when invoked contextually and when
  switching to the Global route and back. Unconfigured-app fallback passed.
- User confirmed action targeting: Finder Snap Left, unconfigured-app Snap Left, and Finder Snap
  Right after switching to Global all moved only the intended window.
- At the user's commit/push/pre-clear request, stopped the acceptance flow before relaunch
  persistence and deletion. Gracefully quit verified PID 50767 and confirmed no MousePlus process
  remained. No installed bundle exists in `/Applications` or `~/Applications`, so none was launched.
  The earlier proposed restart was not performed and does not count as a persistence pass.
- User explicitly included all pending MousePlus work in the commit scope, including retained HUD
  appearance source/tests and Opening Styles documentation. Existing recorded test results remain
  the evidence for those earlier changes; this continuation ran a fresh Debug build, strict deep
  signature verification, live acceptance, and documentation checks, not another automated suite.

## Decisions

- Preserve material beneath the center and every configured wedge. Leave unused positions
  transparent by default, with a shared opt-in to complete the rings; either presentation leaves
  empty geometry inert and absent from accessibility.

## Next

- Relaunch the signed build, verify Finder/Global layouts and triggers survived, then perform profile
  deletion and post-delete fallback acceptance. Keep E1 and the plan open until those pass.

## Resume

- **Commit scope:** All pending MousePlus source, tests, specs, decisions, archived plan, and session
  records are included per the user's explicit instruction, on `fix/hud-live-remediation`.
- **Git handoff:** Work commit `ee4a10e` was pushed to `origin/fix/hud-live-remediation`; the working
  tree was clean and upstream counts were 0 ahead / 0 behind. This final handoff note is committed
  and pushed separately. No MousePlus work is intentionally left uncommitted or unpushed.
- **Validation:** Fresh Debug build and strict deep signature verification passed earlier in this
  continuation. The session-index checker confirms all 29 logs are indexed; `git diff --check`
  passed. No automated test rerun during closeout.
- **Shared guidance:** The Reduce Motion correction is saved locally in the Directions master.
  That separate checkout also contains unrelated edits and existing unpushed commits; it is outside
  the explicitly requested all-pending-MousePlus commit scope.
- **Exact pickup:** Finder selection, both route-switching directions, both same-route dismissals,
  stale-release isolation, center context/drag/Settings, Reduce Motion, and unconfigured-app fallback
  checks plus action targeting passed. Next: relaunch persistence, deletion, and post-delete fallback.
- **App:** Test app is stopped. Reuse the signed artifact at
  `/private/tmp/MousePlus-E1-20260913/Build/Products/Debug/MousePlus.app` on this Mac after checking it
  still exists, or build a fresh signed artifact if needed. Launch the exact app and verify its
  executable path. Then ask the user to confirm the Finder profile, Global layout, shortcuts, and
  customizations survived. Do not delete the Finder profile before persistence acceptance.
- **Outcome:** Paused/incomplete at user-requested session close; no half-done code edit.
- **Deferred:** 5 confirmed backlog items · 0 awaiting reproduction.
- **Model fit:** implementation capability + medium reasoning — the next work is a clear live
  acceptance flow. The current setting is not reliably visible.
