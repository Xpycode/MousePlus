# 2026-09-06 — Opening-style acceptance continuation

## Work and verified results

Continued Task 5.1 at the user's request. Preserved the five pre-existing documentation edits.
No production source or signing configuration changed.

Added `testNativeCenterKeepsItsFrameAndAcceptsAccessibilityPressDuringConcealedOpening` in
`HUDOpeningMotionTests.swift`. It uses the production controller, full renderer and native Settings
button for all six opening choices at 0.5 seconds. It checks nonempty/stable native AX frames and
panel frames during playback, completion, concealment on a new invocation, and synchronous delivery
of exactly one close callback and one Settings callback through native `accessibilityPerformPress`.
Physical hover is disabled to isolate geometry. Callbacks are intercepted so tests do not open the
user's Settings or execute system actions. This is native-control coverage, not VoiceOver traversal.

- Focused suite: **124 passed, 0 failed, 0 skipped**.
  Result: `/tmp/MousePlus-OpeningAcceptance-Focused-20260906.xcresult`.
- Full scheme: **304 passed, 0 failed, 0 skipped**.
  Result: `/tmp/MousePlus-OpeningAcceptance-Full-20260906.xcresult`.
- Both result summaries were inspected with `xcresulttool`; host is arm64 macOS 27.0 (26A5421a).
- Authoritative Debug build passed. Log: `/tmp/MousePlus-OpeningAcceptance-Build-20260906.log`.
- Strict deep signature verification passed outside the sandbox. The sandboxed verification
  reported a trust-service error; that result was not counted as a signature pass.
- Gracefully quit prior PID 59912 and launched PID 85853 from the fresh artifact:
  `/tmp/MousePlus-OpeningCoordinate-Live/Build/Products/Debug/MousePlus.app`.
  Launch helper verified the running executable path. This reuses the prior live artifact path.

## Live observations

The external Swift probe had posting, accessibility and screen-capture access. Through the saved
keyboard shortcut it observed a 472-point square HUD and captured its settled appearance at
`/tmp/MousePlus-OpeningAcceptance-Live.png`. Inspection showed the ring artwork, labels and center
within the panel; this single settled capture does not establish all-style motion quality.

The external AX traversal found `hud.center.settings`, six inner wedges and three middle wedges,
matching the live configuration. The earlier in-process XCTest traversal returned no virtual
SwiftUI children even with Opening Off; that exploratory test was removed. Its result is not an
established app accessibility defect. External enumeration establishes presence only, not stable
virtual frames, ordering or spoken VoiceOver output.

The live `HUDOpening` trace recorded keyboard-triggered Staggered Segments at 0.5 seconds,
concealed mount, intermediate progress and completion with matching native/SwiftUI pointer deltas.
The probe's repeated-shortcut dismissal result was **inconclusive**: it reported a visible window,
while the trace shows additional openings and pointer activity during the check. Captured in TASKS
Inbox for an isolated reproduction; no speculative dismissal change was made.

Probe output: `/tmp/MousePlus-OpeningAcceptance-Live-20260906.log`.
Configuration bytes were unchanged after build/relaunch/probe. Verified saved trigger configuration:
⌃⌥⌘M tap-toggle and direct MousePlus mouse binding disabled. BetterMouse itself was not edited.

## Remaining acceptance

Task 5.1 remains open. A user question requests Circular Sweep, Iris Reveal, Bloom and Staggered
Segments at the fastest and slowest speeds through the working BetterMouse setup. Await actual
feedback; do not infer acceptance from the automated passes.

Still unverified here: spoken VoiceOver and keyboard-only traversal, virtual-wedge frame stability
during opening, live Reduce Motion changes, the full maximum-geometry/screen-edge and branch-change
matrix, smoothness across supported Macs, and physical native mouse triggering. This Mac's Reduce
Motion was off; policy tests cover fallback logic but do not substitute for live observation.

Preserve the BetterMouse mapping and Warp button-5 suppression. Any direct physical-trigger check
must be isolated from Warp without changing that working setup. Do not merge/close the plan based
on this continuation. No commit, fetch, merge or push was performed.

## Pre-clear handoff

The user asked for specific test steps, then requested logging to clear. **No new manual test
results or acceptance were reported.** The 124 focused / 304 full test results above remain the
latest verified runs; logging did not rerun builds/tests or change app behavior.

Project state and today's index row are synced. The active index is capped at 20 rows, with one
older row moved to its archive. The standard drift script reports 17 missing active-index entries
because it does not consult the archive; a combined active/archive check confirms all 27 session
files are linked, with no missing files or orphan links. `git diff --check` passed. No completed
sprint tasks were archived, and the phase is unchanged.

### User testing checklist supplied

Keep the BetterMouse mapping and disabled direct MousePlus binding. In Settings → Ring Appearance
→ Animation, select each of Circular Sweep, Iris Reveal, Bloom and Staggered Segments:

1. At 0.5 seconds, move the pointer away from Settings, summon through BetterMouse, and hold still.
   Check the selected effect, no initial flash, stationary center, no clipping/missing pieces/seams.
2. Repeat all four at 0.05 seconds.
3. At 0.5 seconds, immediately select an action during opening; it should execute exactly once.
4. Without pointer movement, open and press BetterMouse again to close; repeat three times.
   Also check Escape. Run this without concurrent automation.
5. Temporarily enable macOS Reduce Motion; the four new effects should fade. Restore the user's
   preferred system setting afterward.

Start with the four-style visual check; handle VoiceOver separately. For a failure, record the
style, duration and observed behavior. Do not silently expand this into changing native bindings.
Runtime trace saved at `/tmp/MousePlus-OpeningAcceptance-RuntimeTrace-20260906.log`.

## Resume

- **Pickup:** Read this log, `docs/PROJECT_STATE.md`, `docs/plans/hud-opening-styles-implementation.md` Task 5.1, and
  `specs/hud-opening-styles.md`. Collect the user's results from the checklist above. If none are
  available, resume that live check; do not repeat the already-passed suites without a new change,
  failure or specific unresolved concern. Task 5.1, AC3 and AC8–AC9 remain open.
- **Uncommitted work:** Branch `fix/hud-live-remediation`. One new test in
  `01_Project/MousePlusTests/HUDOpeningMotionTests.swift`; changes to `IMPLEMENTATION_PLAN.md`,
  `docs/PROJECT_STATE.md`, `docs/TASKS.md`, `docs/sessions/_index.md`; new log
  `docs/sessions/2026-09-06.md`. The prior `docs/sessions/2026-09-05.md` edit and overlapping
  pre-existing status/plan/task/index edits remain preserved. The index archive is also updated
  to keep the active table at 20 rows. No commit/push was requested or performed.
- **Half-done edits:** None. The retained native-center regression passes. Exploratory virtual AX
  traversal was removed; do not restore its failing assumptions as a proven app defect.
- **Deferred:** 1 confirmed Backlog item (scope App Switcher increment); 1 Inbox observation
  awaiting reproduction (repeated-shortcut dismissal). Separate external dependencies remain
  Logitech captures/strategy and the tip-jar platform/URL. Sprint remains 0/1; overall 35/37 (95%),
  with Inbox excluded by the tracker's progress formula.
- **App:** Last verified fresh signed artifact is
  `/tmp/MousePlus-OpeningCoordinate-Live/Build/Products/Debug/MousePlus.app`, launched as PID 85853
  during verification. No quit/relaunch during this logging pass: retain the user-testing setup
  and standing fresh-build preference. Recheck process identity before future live automation;
  the historical PID is not proof it still runs. Temporary evidence is local to this Mac.
- **Model fit:** Balanced capability with medium reasoning for collecting and recording manual
  acceptance. Use deep capability with high reasoning if a rendering, AX or trigger failure
  requires diagnosis. The current host reasoning setting is not reliably visible.
