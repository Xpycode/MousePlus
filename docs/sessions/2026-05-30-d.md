# Session: 2026-05-30-d

## Goal
Flesh out `docs/APP_COMMANDS_PLAN.md` Phase 0 — and then all of Phases 1–4 — into build-gated `/execute` waves, matching the `IMPLEMENTATION_PLAN.md` format that drove the concentric-ring build.

## Progress
- **Planning/docs only — no code, no build.** Read `APP_COMMANDS_PLAN.md`, `IMPLEMENTATION_PLAN.md` (the wave template), the `/execute` skill, and ground-truthed the referenced source files before writing each wave.
- **Phase 0 — AX foundation → 4 waves / 7 tasks (P0-T1…T7).** W1 models & context (MenuCommand, `ActionType.menuCommand`, FrontmostAppService) ∥; W2 `AccessibilityService` engine; W3 wiring (`ActionService.execute(_:targetApp:)` + show-time snapshot threading); W4 verify (hardcoded Edit▸Paste).
- **Phase 1 — Dynamic RING 3 → 4 waves (P1-T1…T5):** DynamicSource model → async `expand()` w/ stale-guard → pagination ∥ seed wedges → verify.
- **Phase 2 — Profiles + curation → 4 waves (P2-T1…T5):** `Configuration.appProfiles` → show-time resolution ∥ auto-scan ranker → App Commands settings tab → verify.
- **Phase 3 — Center-hub palette → 4 waves (P3-T1…T6):** search engine ∥ panel → view ∥ entry-point → selection/pin handling → verify.
- **Phase 4 — Liquid Glass → 4 waves (P4-T1…T6):** AdaptiveGlass modifier → apply (WedgeView ∥ RingMenuView) → morph ∥ legibility → hardware-gated verify.
- Updated `PROJECT_STATE.md` item 27 to record the plan is now `/execute`-ready end-to-end (recommended start Phase 0; Phase 4 deferred).

## Decisions
*(Engineering/sequencing calls baked into the plan — not new product decisions, so not logged to `decisions.md`.)*
- **Reuse, don't rebuild — corrected the plan against real code:** Phase 0 needs **no new AX-permission plumbing** — `Services/PermissionsService.swift` already does `AXIsProcessTrusted`/prompt/settings-deep-link/polling. HUD §1.4 is therefore already satisfied; the waves consume it.
- **Pinned naming drift:** Phase 1 prose said `WedgeSource`/`.appMenus`; the plan's own §3.2 (and HUD plan) establish `DynamicSource`/`.appMenuMirror`. All waves now use `DynamicSource` + an explicit alignment note so a subagent doesn't spawn a parallel type.
- **Same-file conflicts kept out of shared waves** so `/execute`'s parallel subagents don't collide (e.g. async-expand vs pagination both touch `RingViewModel` → sequenced; pagination ∥ seed-wedges touch different files → parallel). W3 of Phase 0 ships an explicit `execute(_:targetApp:)` contract so its two tasks parallelize against an interface.
- **`.menuCommand` is the executable Phase-0 path; `.menuBar` (full mirror) stays HUD Feature A** — kept as a documented no-op, not silently "killed."
- **Phase 4 hard-gated twice:** macOS 26 **SDK** to compile `glassEffect`/`GlassEffectContainer` (the `#available` guard doesn't rescue an old SDK) **and** macOS 26 **hardware** to verify (no `/preview`; concave-annular glass is undocumented). Sequenced last / deferrable.
- Verify waves honor the `no-synthetic-input-injection` memory — user presses their own trigger.

## Next
- Decide whether to start building **Phase 0** via `/execute` (recommended: AX engine, no UI, kills the `.menuBar` stub, independent of the HID++ decision #17).
- Optional cleanup: the plan still has the original prose lists *above* each new wave table — some redundancy could be trimmed.
- HUD track unchanged: `HUD_ACTIONS_PLAN.md` §1 foundation → Feature B (window snapping) is still the other recommended next build. Phase 0 here and HUD Feature A share the same `AccessibilityService` — whichever lands first satisfies the other.
